/**
 * Team is an individual code-camp team that includes a collection of Bots
 */
import { Bot } from './Bot';
import { ObjectBase } from './ObjectBase';
export declare class Team extends ObjectBase {
    private name;
    private id;
    private logo;
    private bots;
    constructor(data?: Team);
    /**
     * Coerce json bot data into array of Bot objects and return.
     * This enforces data validation and returns concrete, functional
     * Bot objects.
     *
     * @param bots
     */
    private loadBotsArray;
    readonly Id: string;
    Name: string;
    Bots: Array<Bot>;
    Logo: string;
}
