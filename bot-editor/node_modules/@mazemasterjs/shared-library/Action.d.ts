import { COMMANDS, DIRS } from './Enums';
import { Engram } from './Engram';
import { ObjectBase } from './ObjectBase';
import { IAction } from './Interfaces/IAction';
import ITrophyStub from './Interfaces/ITrophyStub';
export declare class Action extends ObjectBase {
    /**
     * Static function to contstruct action directly from JSON
     *
     * @param jsonData
     */
    static fromJson(jsonData: IAction): Action;
    command: COMMANDS;
    direction: DIRS;
    message: string;
    engram: Engram;
    outcomes: Array<string>;
    score: number;
    moveCount: number;
    trophies: Array<ITrophyStub>;
    botCohesion: Array<number>;
    constructor(command: COMMANDS, direction: DIRS, message: string);
    /**
     * Returns a simple JSON stub of Action data
     */
    getStub(): object;
}
